rapidjson = {}

rapidjson.null = nil

function  rapidjson.decode(json_string)
    
end

--[[
    value : 
        table|true|false|number|rapidjson.null
    option : 
        pretty = true|false
        sort_keys = true|false
        empty_table_as_array = true|false
--]]
function rapidjson.encode(value, option)
    
end

function rapidjson.load(file_name)
    
end

function rapidjson.dump(value, file_name, option)
    
end

function rapidjson.object(t)
    
end

function  rapidjson.array(t)
    
end

function  rapidjson.Document(value)
    
end

rapidjson.document = {}

function rapidjson.document.parse(str)
    
end

function rapidjson.document.get(key, default_val)
    
end

function rapidjson.document.set(key, value)
    
end

function rapidjson.SchemaDocument(value)
    
end

function rapidjson.SchemaValidator(doc)
    
end

rapidjson.validator = {}



